import { Placeholder } from '@/components';

export default function CheckinScreen() {
  return (
    <Placeholder
      title="Ton check-in du soir"
      subtitle="Humeur, une question honnête, déclaration de contact — puis ton insight du jour, différent chaque soir. On le branche dans le lot rétention."
    />
  );
}
